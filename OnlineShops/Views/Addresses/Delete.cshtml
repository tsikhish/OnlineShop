@model OnlineShops.Models.Address

@{
    ViewData["Title"] = "Delete Address";
}

<h2>Delete Address</h2>

<h3>Are you sure you want to delete this address?</h3>

<div>
    <dl class="row">
        <dt class="col-sm-2">Address Line 1</dt>
        <dd class="col-sm-10">@Model.AddressLine1</dd>

        <dt class="col-sm-2">Address Line 2</dt>
        <dd class="col-sm-10">@Model.AddressLine2</dd>

        <dt class="col-sm-2">City</dt>
        <dd class="col-sm-10">@Model.City</dd>

        <dt class="col-sm-2">State/Province</dt>
        <dd class="col-sm-10">@Model.StateProvince</dd>

        <dt class="col-sm-2">Country/Region</dt>
        <dd class="col-sm-10">@Model.CountryRegion</dd>

        <dt class="col-sm-2">Postal Code</dt>
        <dd class="col-sm-10">@Model.PostalCode</dd>
    </dl>

    <form asp-action="DeleteConfirmed" method="post">
        <input type="hidden" asp-for="AddressId" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>